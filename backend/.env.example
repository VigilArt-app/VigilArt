# Back-end
SERVER_PORT=8000
SALT_ROUNDS=
SECRET_JWT=""
API_PREFIX="/api/v1"

# Database
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
DB_NAME=
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}"