{
  "name": "@vigilart/backend",
  "version": "0.0.1",
  "dependencies": {
    "@nestjs/core": "^11.1.8",
    "@nestjs/common": "^11.1.8",
    "@vigilart/shared": "workspace:*"
  },
  "scripts": {
    "dev": "doppler run -- nest start --watch",
    "build": "tsc -p tsconfig.json",
    "start": "doppler run --config prod -- node dist/main.js"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.10"
  }
}