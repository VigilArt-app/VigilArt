// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider     = "prisma-client"
  output       = "../../shared/src/generated/prisma"
  engineType   = "client"
  moduleFormat = "cjs"
}

generator zod {
  provider          = "zod-prisma-types"
  output            = "../../shared/src/generated/zod"
  prismaClientPath  = "../prisma/client"
  createInputTypes  = false
  addIncludeType    = false
  useTypeAssertions = true
}

datasource db {
  provider = "postgresql"
}

// User Models & Enums

model User {
  id               String           @id @default(uuid())
  email            String           @unique @db.VarChar(254)
  password         String
  firstName        String           @db.VarChar(64)
  lastName         String           @db.VarChar(64)
  avatar           String
  createdAt        DateTime         @default(now())
  subscriptionTier SubscriptionTier
  artworks Artwork[]
}

enum SubscriptionTier {
  FREE
  CREATOR
  PRO
}

enum MatchType {
  FULL
  PARTIAL
}

model Artwork {
  id String @id @default(uuid())
  userId String
  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  imageUri String
  originalFilename String?
  contentType String?  // "image/png", "image/jpeg"
  sizeBytes Int?
  description String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  lastScanAt DateTime?
  // reports ArtworkIndividualReport[]
}

// model ArtworkStatistics {
//   id String @id @default(uuid())

//   // report ArtworkIndividualReport?
//   totalMatches Int
// }

// model ArtworkMetadataLabel {
//   id String @id @default(uuid())
//   artworkMetadataId String
//   artworkMetadata ArtworkMetadata @relation(fields: [artworkMetadataId], references: [id], onDelete: Cascade)

//   label String
//   languageCode String?
// }

// model ArtworkWebEntity {
//   id String @id @default(uuid())
//   artworkMetadataId String
//   artworkMetadata ArtworkMetadata @relation(fields: [artworkMetadataId], references: [id], onDelete: Cascade)

//   score Int
//   description String
// }

// model ArtworkMetadata {
//   id String @id @default(uuid())

//   report ArtworkIndividualReport?
//   bestGuessLabels ArtworkMetadataLabel[]
//   webEntities ArtworkWebEntity[]
// }

enum WebsiteCategory {
  SOCIAL
  ART_PLATFORMS
  MARKETPLACES
  BLOG
  MEDIA
  SEARCH
  OTHER
}

// model MatchingPage {
//   id String @id @default(uuid())  
//   artworkReportId String
//   artworkReport ArtworkIndividualReport @relation(fields: [artworkReportId], references: [id], onDelete: Cascade)

//   matchType MatchType
//   url String
//   pageTitle String?
//   category WebsiteCategory
//   websiteName String
//   imageUrl String?
// }

// model ArtworkIndividualReport {
//   id String @id @default(uuid())
//   artworkId String
//   artwork Artwork @relation(fields: [artworkId], references: [id], onDelete: Cascade)

//   // detectionDate DateTime
//   // statisticsId String @unique
//   // statistics ArtworkStatistics @relation(fields: [statisticsId], references: [id], onDelete: Cascade)
//   metadataId String @unique
//   metadata ArtworkMetadata @relation(fields: [metadataId], references: [id], onDelete: Cascade)

//   matchingPages MatchingPage[]
// }

// TO UPDATE /!\
//make model for scan report of each artwork and of all artworks
