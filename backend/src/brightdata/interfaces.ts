export interface GoogleLensExactResult {
  title?: string;
  link?: string;
  extensions?: [string];
  source?: string;
  logo?: string;
  image?: string;
  image_url?: string;
  image_base64?: string;
  rank?: number;
  global_rank?: number;
}
