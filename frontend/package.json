{
  "name": "@vigilart/frontend",
  "version": "0.0.1",
  "dependencies": {
    "@vigilart/shared": "workspace:*",
    "next": "^16.0.1",
    "react": "^19.2.0"
  },
  "scripts": {
    "dev": "doppler run --config dev -- next dev",
    "build": "doppler run --config prod -- next build",
    "start": "doppler run --config prod -- next start"
  },
  "devDependencies": {
    "@types/node": "24.10.0",
    "@types/react": "19.2.2"
  }
}
